services:
  pli:
    build:
      context: ./pli
    platform: linux/386
    image: ghcr.io/ross-shk/pli
    security_opt:
      - seccomp=unconfined
    stdin_open: true
    tty: true
    working_dir: /pli-dev
    command: /bin/bash
